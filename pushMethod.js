let listOfNames = ['Alex', 'nicolle', 'mario', 'dad','mom'];

console.log(listOfNames.push('deivid')); // 6

console.log(listOfNames); // ['Alex', 'nicolle', 'mario', 'dad','mom', 'deivid']


/* line 1 is declaring a variable listOfNames and initializing it to an array with a string
of elements. on line 3 we are outputing 6 to console because we are using the push method on
`listOfNames` with a parameter `deivid`
*/