// on line 1 we are declaring a variable named foo and intizaling it to an array ['a', 'b', 'c']
// on line 2 we then console.log the foo with the property .length and outputing 3
// on line 3 we then console.log foo with the index[3] and get undefinded not an ERROR 

let foo = ['a', 'b', 'c'];
console.log(foo.length);  // => 3
console.log(foo[3]);      // will this result in an error?
